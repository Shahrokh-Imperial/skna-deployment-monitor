# Large files

For large recordings, use **Local recording file** and enter a filesystem path. Browser upload is configured up to 2 GB but remains limited by browser, memory, and server resources. Parsed recordings are cached so changing controls does not re-read the source file.
