# Synthetic deployment examples

These three generated events demonstrate the bundled-event mode. They are not Pig 667 data and do not reproduce manuscript values. The app recalculates each adaptive threshold from up to 60 s immediately before onset and applies the public replay defaults.
